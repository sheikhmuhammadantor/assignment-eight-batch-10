import PropTypes from 'prop-types'

function Wishlist() {
  return (
    <div>
      Wish.........
      {/* <div className="w-full flex flex-col gap-8 mt-12">
        {
          wishData.map((product) => <Cart wish={false} key={product.product_id} product={product}></Cart>)
        }
      </div> */}
    </div>
  )
}

Wishlist.propTypes = {

}

export default Wishlist

